using System.Reflection;

// Kept in sync with ModInfo.xml <Version>. Stamped here (not via the SDK's
// GenerateAssemblyInfo, which is off) so both the dotnet and mcs build backends
// carry it and ModApi's startup version record reports a real version.
[assembly: AssemblyVersion("1.13.0.0")]
[assembly: AssemblyFileVersion("1.13.0.0")]
